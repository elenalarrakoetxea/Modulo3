@model IEnumerable<Autor1a1.Models.Biografia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaEdicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (Biografia biografia in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => biografia.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => biografia.FechaEdicion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => biografia.Autor.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => biografia.Autor.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@biografia.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@biografia.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@biografia.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
